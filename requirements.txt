# 財報 RAG 系統（version8）依賴套件
# 版本為驗證環境（conda env: financial_crawler, Python 3.10）實測鎖定；
# 相容性允許時可放寬為 >= 下限。
#
# 安裝：pip install -r requirements.txt
# 另需：playwright install chromium   （無頭渲染基準測試用）

# ── 核心引擎（rag_test_system_v14 / rag_api）─────────────────
torch==2.9.1
pandas==2.3.3
numpy==1.23.5
tabulate==0.9.0

# ── 向量檢索軌（build-index / vector_search）──────────────────
chromadb==1.5.9
sentence-transformers==5.2.0
transformers==4.56.0

# ── LLM 服務（意圖路由 + 生成；伺服器端）─────────────────────
# vLLM 以獨立服務啟動：
#   vllm serve Qwen/Qwen3-4B-AWQ --gpu-memory-utilization 0.85
vllm==0.16.0

# ── 評估指標（Module 4 evaluate）─────────────────────────────
bert-score==0.3.13
jieba==0.42.1
pyemd==1.1.0          # moverscore_v2（本地模組）相依
nvidia-ml-py==13.590.44   # GPU / VRAM 效能剖析
pynvml==13.0.1

# ── 前端展示（rag_frontend / neo4j_graph_demo）───────────────
Flask==3.1.3
neo4j==6.2.0          # 圖形資料庫 demo 與延遲基準測試
playwright==1.57.0    # 無頭自動化渲染對照實驗

requests==2.34.2
